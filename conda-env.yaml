name: quarto_docs
channels:
  - conda-forge
dependencies:
  - python
  # docs/presentation deps:
  - quarto
  - pip
  - pyyaml
  - nbformat
  - nbclient
  - jupyter_client
  - ipykernel
  - pip:
      - pipdeptree
      - python-pptx
      - bash_kernel
