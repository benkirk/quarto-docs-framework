name: quarto_docs
channels:
  - conda-forge
dependencies:
  # Binaries / runtime — must come from conda.
  - python
  - quarto
  - gh
  - make
  - pip
  # Pure-python deps install much faster as wheels via pip than via the
  # conda solver, and don't add to the solve graph.
  - pip:
      - bash_kernel
      - ipykernel
      - jupyter_client
      - nbclient
      - nbformat
      - pipdeptree
      - python-pptx
      - pyyaml
